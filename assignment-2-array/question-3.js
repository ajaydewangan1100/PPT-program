let array = [4, 3, 3, 7, 6, 6]

let find = (arr) => {
    arr = arr.sort((a, b) => a - b);
    let count = [];
    for (let i = 1; i < arr.length; i += 2) {
        
    }

    return count
}
let result = find(array);

console.log(result)